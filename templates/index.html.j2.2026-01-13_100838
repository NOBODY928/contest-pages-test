<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>竞赛资讯聚合</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    body { font-family: Arial,"PingFang SC","Microsoft YaHei",sans-serif; margin: 24px; }
    .meta { color:#666; margin-bottom:16px; }
    .item { padding:10px 0; border-bottom:1px solid #eee; }
    .src { display:inline-block; font-size:12px; padding:2px 6px; border:1px solid #ddd; border-radius:999px; margin-right:8px; color:#444;}
    .date { color:#888; font-size:12px; margin-left:8px; }
    a { color:#111; text-decoration:none; }
    a:hover { text-decoration:underline; }
  </style>
</head>
<body>
  <h1>竞赛资讯聚合</h1>
  <div class="meta">生成时间：{{ generated_at }}；数据源为公开页面抓取整理。</div>

  {% if items and items|length > 0 %}
    {% for it in items %}
      <div class="item">
        <span class="src">{{ it.source_name }}</span>
        <a href="{{ it.link }}" target="_blank" rel="noopener">{{ it.title }}</a>
        {% if it.date %}<span class="date">{{ it.date }}</span>{% endif %}
      </div>
    {% endfor %}
  {% else %}
    <p>暂无数据。</p>
  {% endif %}

  <hr />
  <div class="meta">© zihguo.me</div>
  <!-- generated_at: {{ generated_at }} -->
</body>
</html>
